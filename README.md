# TaskFlow - Управление задачами нового уровня

TaskFlow — это современное веб-приложение для управления списками задач, созданное с использованием передовых технологий React и TypeScript. Приложение предлагает интуитивный интерфейс для создания, организации и отслеживания задач с поддержкой множественных списков, фильтрации и пагинации.

## ✨ Ключевые особенности

- **Множественные списки задач** — создавайте неограниченное количество списков для организации ваших задач
- **Безопасная аутентификация** — защищенный вход с автоматической проверкой авторизации
- **Адаптивные темы** — переключение между темной и светлой темой для комфортной работы в любое время
- **Полностью адаптивный дизайн** — идеальная работа на всех устройствах от мобильных до десктопов
- **Высокая производительность** — оптимизированная загрузка с skeleton screens и кэшированием данных
- **Умная фильтрация** — фильтруйте задачи по статусу (Все, Активные, Выполненные)
- **Пагинация задач** — удобная навигация по большим спискам задач

## 🚀 Технологический стек

- **React 19.1.0** - библиотека для построения пользовательского интерфейса
- **TypeScript 5.8.3** - типизированный JavaScript
- **Vite 7.0.0** - инструмент сборки и разработки
- **Redux Toolkit 2.8.2** - управление состоянием приложения
- **RTK Query** - работа с API и кэширование данных
- **React Router 7.6.2** - маршрутизация
- **Material-UI 7.1.2** - компоненты пользовательского интерфейса
- **React Hook Form 7.58.1** - управление формами
- **Zod 3.25.67** - валидация схем данных
- **Emotion** - CSS-in-JS решение для стилизации

## 📋 Функциональность

### Аутентификация
- Вход в систему (логин/пароль)
- Автоматическая проверка авторизации при загрузке приложения
- Защищенные маршруты
- Выход из системы

### Управление списками задач
- Создание новых списков задач
- Редактирование названий списков
- Удаление списков задач
- Фильтрация задач по статусу (All, Active, Completed)

### Управление задачами (Tasks)
- Создание задач в списках
- Редактирование текста задач
- Изменение статуса выполнения задач
- Удаление задач
- Пагинация задач (по 4 задачи на страницу)

### Дополнительные возможности
- Переключение между темной и светлой темой (серые оттенки для светлой темы)
- Отображение ошибок через snackbar
- Skeleton-загрузка для улучшения UX
- Полностью адаптивный дизайн:
  - **Большие экраны (md+)**: 3 списка задач в ряд на всю ширину
  - **Планшеты (sm)**: 2 списка задач в ряд
  - **Мобильные (xs)**: 1 список задач на всю ширину
- Плавные анимации и переходы
- Современный UI/UX дизайн с Material-UI компонентами
- Оптимизированная производительность без горизонтального скролла

## 🛠️ Установка и запуск

### Предварительные требования

- Node.js (рекомендуется версия 18 или выше)
- pnpm (менеджер пакетов)

### Установка зависимостей

```bash
pnpm install
```

### Настройка переменных окружения

Создайте файл `.env` в корне проекта со следующими переменными:

```env
VITE_BASE_URL=https://social-network.samuraijs.com/api/1.1
VITE_API_KEY=your-api-key-here
```

**Примечание:** Для получения API ключа необходимо зарегистрироваться на [social-network.samuraijs.com](https://social-network.samuraijs.com)

### Тестовые учетные данные

Для быстрого тестирования приложения можно использовать следующие учетные данные:
- **Email:** free@samuraijs.com
- **Password:** free

### Запуск приложения в режиме разработки

```bash
pnpm dev
```

Приложение будет доступно по адресу: http://localhost:3000

### Сборка для production

```bash
pnpm build
```

Собранные файлы будут находиться в папке `dist/`.

### Предпросмотр production сборки

```bash
pnpm preview
```

### Деплой на GitHub Pages

```bash
pnpm deploy
```

Этот скрипт автоматически выполнит сборку и задеплоит приложение на GitHub Pages.

## 📁 Структура проекта

```
src/
├── app/                    # Основная логика приложения
│   ├── App.tsx            # Корневой компонент приложения
│   ├── Main.tsx           # Главная страница с списками задач
│   ├── app-slice.ts       # Redux slice для глобального состояния
│   ├── baseApi.ts         # Базовая конфигурация RTK Query
│   └── store.ts           # Redux store
│
├── common/                 # Общие компоненты и утилиты
│   ├── components/        # Переиспользуемые компоненты
│   │   ├── CreateItemForm/    # Форма создания элементов
│   │   ├── EditableSpan/      # Редактируемый текст
│   │   ├── ErrorSnackbar/     # Компонент отображения ошибок
│   │   ├── Header/            # Шапка приложения
│   │   ├── NavButton/         # Кнопка навигации
│   │   ├── PageNotFound/      # Страница 404
│   │   └── ProtectedRoute/    # Защищенный маршрут
│   ├── constants/         # Константы приложения
│   ├── enums/             # Перечисления
│   ├── hooks/             # Кастомные хуки (useAppDispatch, useAppSelector)
│   ├── routing/           # Конфигурация маршрутизации
│   ├── styles/            # Общие стили (cardSx, containerSx, skeletonContainerSx)
│   ├── theme/             # Тема Material-UI (темная/светлая)
│   ├── types/             # Общие типы TypeScript
│   └── utils/             # Утилиты (обработка ошибок)
│
└── features/              # Функциональные модули
    ├── auth/              # Модуль аутентификации
    │   ├── api/          # API endpoints для авторизации
    │   ├── lib/          # Схемы валидации (Zod)
    │   └── ui/           # UI компоненты (Login)
    │
    └── todolists/        # Модуль списков задач
        ├── api/          # API endpoints для todolists и tasks
        ├── lib/          # Типы и утилиты
        └── ui/           # UI компоненты
            └── Todolists/
                ├── TodolistItem/    # Компонент одного списка
                │   ├── FilterButtons/    # Кнопки фильтрации
                │   ├── Tasks/           # Компонент списка задач
                │   │   ├── TaskItem/         # Компонент одной задачи
                │   │   ├── TasksPagination/  # Пагинация задач
                │   │   └── TasksSkeleton/    # Skeleton загрузки
                │   └── TodolistTitle/   # Заголовок списка
                └── TodolistSkeleton/    # Skeleton загрузки списка
```

## 🔧 Основные скрипты

- `pnpm dev` - запуск приложения в режиме разработки
- `pnpm build` - сборка приложения для production
- `pnpm preview` - предпросмотр production сборки
- `pnpm test` - запуск тестов (Vitest)
- `pnpm deploy` - деплой на GitHub Pages

## 🎨 Архитектура

### State Management

Приложение использует Redux Toolkit для управления состоянием:
- **app-slice** - глобальное состояние (тема, статус авторизации, статус загрузки, ошибки)
- **RTK Query** - кэширование и синхронизация данных с сервером
- Автоматическая инвалидация кэша при мутациях

### API Integration

Все API запросы обрабатываются через RTK Query с автоматическим:
- Кэшированием данных
- Инвалидацией кэша при мутациях
- Оптимистичными обновлениями
- Централизованной обработкой ошибок
- Автоматической отправкой токена авторизации

### Routing

Используется React Router с защищенными маршрутами:
- `/` - главная страница (требует авторизации)
- `/login` - страница входа (доступна только неавторизованным)
- `*` - страница 404

### Form Handling

Формы управляются через React Hook Form с валидацией через Zod схемы:
- Валидация на клиенте перед отправкой
- Отображение ошибок валидации
- Поддержка капчи при необходимости

### Styling

Приложение использует комбинированный подход к стилизации:
- **Material-UI (MUI)** - основные компоненты UI
- **CSS Modules** - для компонентных стилей
- **Emotion (sx prop)** - для динамических стилей
- **Единая система стилей** - централизованные стили в `common/styles`
- **Адаптивный дизайн** - использование breakpoints MUI (xs, sm, md, lg)
- **Темная/светлая тема** - полная поддержка обеих тем с серыми оттенками

### Responsive Design

Приложение полностью адаптивно:
- Использование Grid системы Material-UI
- Адаптивные отступы и размеры шрифтов
- Оптимизация для мобильных устройств
- Предотвращение горизонтального скролла

## 🔐 Безопасность

- Токен авторизации хранится в `localStorage`
- API ключ передается через заголовки запросов
- Защищенные маршруты проверяют статус авторизации
- Автоматическая проверка авторизации при загрузке приложения
- Защита от XSS через валидацию входных данных

## 📝 Принципы разработки

Проект следует принципам:
- **SOLID** - принципы объектно-ориентированного программирования
- **KISS** - простота и понятность кода
- **DRY** - избегание дублирования кода

### Соглашения о коде

- Использование только функциональных компонентов React
- Каждый компонент в отдельном файле с расширением `.tsx`
- Создание `index.ts` для экспорта компонентов
- Использование `const` и `let` вместо `var`
- Приоритет чистого, эффективного и поддерживаемого кода
- Централизация стилей для избежания дублирования
- Использование TypeScript для строгой типизации
- Следование Feature-Sliced Design архитектуре

## 🌐 API

Приложение использует REST API для работы с данными:
- Базовый URL настраивается через переменную окружения `VITE_BASE_URL`
- Все запросы включают API ключ в заголовках (`API-KEY`)
- Авторизация через Bearer токен в заголовке `Authorization`
- Автоматическая обработка ошибок с отображением через snackbar
- Поддержка капчи при необходимости (автоматическое отображение при ошибке авторизации)

## 📦 Зависимости

Основные зависимости перечислены в `package.json`. Для установки используйте:

```bash
pnpm install
```

## 🎯 Особенности реализации

### Производительность
- Оптимизированная загрузка с использованием skeleton screens
- Кэширование данных через RTK Query
- Предотвращение горизонтального скролла
- Адаптивные изображения и компоненты

### UX/UI
- Плавные анимации и переходы
- Интуитивно понятный интерфейс
- Отзывчивая обратная связь на действия пользователя
- Адаптивный дизайн для всех устройств

### Код
- Чистая архитектура с разделением на features
- Переиспользуемые компоненты
- Централизованная система стилей
- Строгая типизация TypeScript

## 🤝 Вклад в проект

При внесении изменений:
1. Убедитесь, что код следует установленным принципам (SOLID, KISS, DRY)
2. Проверьте, что не сломана существующая функциональность
3. Используйте TypeScript для типизации
4. Следуйте структуре проекта (Feature-Sliced Design)
5. Убедитесь, что нет горизонтального скролла
6. Проверьте адаптивность на разных размерах экрана
7. Используйте централизованные стили из `common/styles`

## 📄 Лицензия

Проект создан в образовательных целях.
